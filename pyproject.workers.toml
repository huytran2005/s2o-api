[project]
name = "s2o-api-workers"
version = "0.1.0"
description = "Cloudflare Workers Python entrypoint for S2O API"
requires-python = ">=3.11"

[tool.wrangler]
name = "s2o-api-workers"
main = "worker.py"
type = "python"
